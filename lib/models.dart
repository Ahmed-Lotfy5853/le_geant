import 'package:flutter/material.dart';

class Exam {
    List<DataRow> DatRow=[];

  /*Exam({
    required  this.Name,
    required this.Date,
    required this.Degree,
    required this.FinalDegree
  });
  factory Exam.fromJson(Map<String,dynamic>json)=>Exam(Name: json["data"][0]["full_exam"][0]["exam"]["exam_name"], Date: json["data"][0]["full_exam"][0]["exam"]["time_ago"], Degree: json["data"][0]["full_exam"][0]["mark"],FinalDegree: json["data"][0]["full_exam"][0]["fullmark"], );
*/}
